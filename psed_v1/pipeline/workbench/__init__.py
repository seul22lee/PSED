"""The production comparison Workbench: consumes a semantic corpus
(papers/<id>/semantic + canonical curves) through an explicit corpus root and
work list, and builds the scientific comparison model, validation and page."""
