"""The production semantic layer: papers/<id>/{extracted,resolved,canonical}
-> papers/<id>/semantic/*.json (ExperimentalCase, Measurement, ResultSeries,
representations, links, evidence...). The scientific behaviour is the one
validated by the semantic pilot and Workbench v2."""
