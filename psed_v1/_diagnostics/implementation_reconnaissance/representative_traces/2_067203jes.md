# `10.1149_2.067203jes` — implementation trace

Ground truth: the paper deposits SiO2 (planar and in AR~30 high-aspect-ratio trenches) and
an Al2O3 capping layer, and reports SiO2/Al2O3 stack results.

## Material

`scout.json` `materials = ["SiO2", "Al2O3"]` -> `multi_material_paper = True` on all 38
entities.

`chemistry_scope.resolve_material` walks its 7-rung ladder per record. Rung 6
(`paper_single_material`) requires `len(set(materials)) == 1` and therefore **never fires
for this paper**. Rung 7 refuses.

```
entities (38)   : SiO2 18, None 20
                  scope: unresolved 20, panel_caption_clause 7, series_legend 6,
                         figure_caption 4, figure_scout_note 1
experiments (32): SiO2 21, None 11
                  scope: series_legend 13, unresolved 11, figure_caption 4,
                         panel_caption_clause 4
```

**No paper-level broadcast occurs.** The resolver refuses on 20 of 38 entities rather than
picking one of the two materials. Al2O3 appears nowhere in the resolved layer because no
figure/panel/legend named it in a way the ladder could resolve — not because SiO2 overwrote
it. `material_candidates` and `material_ambiguity_reason` are retained on the 20 unresolved
entities.

Selection site: `to_kb.py:1766-1771` copies the ladder's result onto `e{}`; `to_kb.py:1001-1006`
copies it onto the entity.

## Geometry

`extracted/geometry.json` = `{geometry_class: "planar", structure: "planar_wafer",
method: "deterministic", evidence: "no 3D test structure found"}`.

`geometry.tag_experiments` (`geometry.py:249-267`) then writes
`e["geometry_class"] = "planar"` onto **all 32** experiment records, overwriting whatever
resolve had put there. The HAR trench cases are not distinguishable at any layer; there is
no per-figure geometry evidence in the pipeline to lose, because
`classify_deterministic` runs once per document.

Selection site: `geometry.py:255` (`g.get("geometry_class", "planar")` — note the literal
`"planar"` default) and `geometry.py:263`.

## Where local evidence is discarded or overwritten

| field | discarded/overwritten | site |
|---|---|---|
| geometry | **overwritten**, paper value onto every experiment | `geometry.py:263` |
| material | **not overwritten** — refused and recorded as `unresolved` with candidates | `chemistry_scope.py` rung 7 |
| `cls["signals"]`, `supported_setting_count` | discarded | `to_kb.py:968-1035` (never copied) |

## Experiment identity

32 experiments across 8 panels of printed Fig 4 and others; `figure_slug = None` on all 32
(the field is set to `None` at `to_kb.py:1140` and never filled for case records).
Classification: `continuous_trace` 15, `discrete_experimental_sweep` 9,
`multi_output_measurement` 8.

Fig 4's eight printed panels each yield their own `Fig4<letter>` id family via
`assign_experiment_ids`; nothing groups them.
