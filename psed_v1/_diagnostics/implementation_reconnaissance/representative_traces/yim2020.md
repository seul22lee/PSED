# Yim 2020 (`10.1039_d0cp03358h`) — implementation trace

Live corpus paper. 70 entities, 39 experiments, 0 series, `material = "Al2O3"` (58
`figure_caption`, 9 `panel_caption_clause`, 3 `figure_body`), `geometry_class =
"lateral_channel"` (`pillarhall_lhar`).

## Entities and experiments by printed figure

| printed fig | entities | classes | representations | cases | experiments |
|---|---|---|---|---|---|
| 3 | 2 | `simulation` 1, `experimental_profile` 1 | `scaled` | 1 | 1 |
| 5 | 3 | `multi_output_measurement` 3 | `primary` | 1 | 3 |
| 7 | 3 | `experimental_profile` 3 | `primary` | 3 | 3 |
| 8 | 8 | `experimental_profile` 8 | `primary` | 8 | 8 |
| 9 | 18 | `experimental_profile` 18 | `as_measured`, `scaled`, `normalized` | 18 | 18 |
| 10 | 30 | `simulation` 30 | `as_measured`, `scaled`, `normalized` | **0** | **0** |
| 11 | 6 | `experimental_profile` 6 | `primary` | 6 | 6 |

Totals: 70 entities, 39 experiments (31 `SimulationRun` entities mint nothing).

## Per-case trace

### Series A — "All of the films were grown in the same ALD run"
- **Ground-truth distinction:** one deposition run, three samples.
- **Fields available:** the phrase is in `document.md`; `SAMPLE_ID` matches `Series A` in
  captions (regex alternative `\bSeries\s+([A-Z])\b`, `entities.py:71`). Corpus-wide the
  `I` family fires on 268 entities across 24 papers.
- **Code branch:** `entities.classify` L331 sets `sig["I"]`; `_result` L611 returns
  `signal_families` (letters only); `to_kb.py:976` persists the letters. `cls["signals"]`
  (with the matched text) is not persisted.
- **Emitted:** separate `experimental_profile` entities, one case each. No run object.

### Series B — "effect of spot size ... 10 and 5 objective lenses" (Fig 7a)
- **Ground-truth distinction:** a measurement setting, not a deposition case.
- **Fields available:** the objective is not an ontology QuantityKind, so it produces no
  assertion; the x axis is read as an independent process sweep.
- **Code branch:** `granularity.classify` -> `independent_process_sweep`;
  `to_kb.py:1057-1058` mints one case per distinct x value.
- **Emitted:** 3 Experiments.

### Sample 11 — one specimen, three techniques
- **Ground-truth distinction:** one sample, three measurements.
- **Fields available:** `SAMPLE_ID` returns `sample 11 surface` from the caption.
- **Code branch:** the three curves are `multi_output_measurement` in one panel, so
  `_events` (L1210-1230) groups them: holder keeps 1 case, the other two are forced to
  `experimental_case_count = 0`, `status = "shared_measurement_event"`, and receive
  `shares_physical_case_with = holder.entity_id`.
- **Emitted:** 3 Experiments, 1 case, 1 `physical_case_id`, 1 `measurement_event_id`.
  This is the one place the current model does express sample sharing — and only because
  all three curves are in the same panel.

### Fig 8a — repeat measurement, "(sample 8 in Table 1)"
- **Ground-truth distinction:** one sample measured repeatedly.
- **Fields available:** `SAMPLE_ID` returns `sample 8 in Table 1`.
- **Code branch:** `experimental_profile`, `CLASS_MODEL["case"] == 1` unconditionally
  (`to_kb.py:1040-1041`).
- **Emitted:** 4 of the 8 Fig 8 Experiments; each its own case and `physical_case_id`.

### Fig 8b — different ALD runs, one condition case ("reproducibility of ALD runs")
- **Ground-truth distinction:** several runs under one recipe.
- **Fields available:** `reproducibility` appears in `document.md` (12 hits corpus-wide,
  11 of 44 papers carry run/replicate/reproducibility phrasing at all).
- **Code branch:** same as Fig 8a — `experimental_profile`, one case each.
- **Emitted:** the other 4 Fig 8 Experiments.

### Fig 9 — three representations x two sample sets
- **Ground-truth distinction:** 6 measurements shown 3 ways each... 18 panels of drawn data.
- **Fields available:** `representation` correctly detected on all 18
  (`a`/`d` = `as_measured`, `b`/`e` = `scaled`, `c`/`f` = `normalized`).
- **Code branch:** `to_kb.py:1040` `CLASS_MODEL["experimental_profile"]["case"] == 1`.
  **No branch reads `representation`.**
- **Emitted:** 18 entities, 18 cases, 18 `physical_case_id`s, 18 canonical curves.
  `test_stage0_regression.py:141` ("representations do not duplicate the underlying case")
  passes, because it asserts `experimental_case_count <= 1` **per entity**.

### Fig 10 — MATLAB re-implementation of the Ylilammi model
- **Ground-truth distinction:** simulation.
- **Fields available:** `panel_source` flag, `SIM_LABEL` on series labels, body text.
- **Code branch:** `entities.classify` -> `simulation`; `CLASS_MODEL["simulation"]["case"] == 0`,
  `is_experiment False`; gates at L1039 and L1127 both reject.
- **Emitted:** 30 `SimulationRun` entities, **0 experiments**, canonical
  `data_source = "simulated"`. **Correct.**

### Fig 11 — genuine per-condition sweeps (Series E / F)
- **Ground-truth distinction:** distinct deposition condition cases.
- **Code branch:** `experimental_profile`, one case each; `_figure_varied` (L617-641)
  prevents panel (a)'s pulse time from being broadcast to panel (b)'s purge-time panel.
- **Emitted:** 6 Experiments. **Correct.**

## Summary of code branches involved

| branch | file:line | figures it decides |
|---|---|---|
| `CLASS_MODEL["experimental_profile"]["case"] == 1` | `entities.py:634-635` + `to_kb.py:1040` | 3, 7, 8, 9, 11 |
| `CLASS_MODEL["simulation"]["is_experiment"] is False` | `entities.py:638-639` + `to_kb.py:1039` | 10 |
| `_events` multi-output grouping | `to_kb.py:1210-1230` | 5 |
| `granularity == "independent_process_sweep"` | `to_kb.py:1049-1058` | 7 |
| `assign_experiment_ids` figure/panel slug | `to_kb.py:793-828` | all |
