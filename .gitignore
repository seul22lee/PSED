.env
__pycache__/
.env
__pycache__/
old/extract-line-chart-data/
psed_v1/03_corpus/corpus_status.html

# Generated at test time and stamped with the commit it ran against. Tracking it made the
# contract unsatisfiable: committing the file creates a new HEAD, so the SHA it records is
# stale the moment it lands. It is runtime state about a checkout, not repository content.
psed_v1/_diagnostics/semantic_pilot_9papers/logs/test_status.json
